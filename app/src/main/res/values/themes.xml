<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The host snapshot wraps the resource context; unavailable settings follow the system. -->
    <style name="Theme.AiAgent" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar" />
    <style name="Theme.AiAgent.Light" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar" />
    <style name="Theme.AiAgent.Dark" parent="@android:style/Theme.DeviceDefault.NoActionBar" />
</resources>
